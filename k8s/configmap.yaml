# Shared configuration consumed by all services via envFrom.
apiVersion: v1
kind: ConfigMap
metadata:
  name: sepsisai-config
  namespace: sepsisai
data:
  MONGO_HOST: "mongodb"
  MONGO_PORT: "27017"
  MONGO_DB: "SepsisTraining"
  AI_SERVICE_HOST: "ai-prediction"
  AI_SERVICE_PORT: "8000"
