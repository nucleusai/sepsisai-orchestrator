apiVersion: v1
kind: Service
metadata:
  name: ai-prediction
  namespace: sepsisai
spec:
  type: NodePort
  selector:
    app: ai-prediction
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30001
